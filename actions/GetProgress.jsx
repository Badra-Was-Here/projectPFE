
import connectMongoDB from "@/lib/mongodb";


export default async function GetProgressuser(userId,courseId) {
    try {
        
    } catch (error) {
        console.log("[GET_PROGRESS]", error);
        return 0;
    }
}
